version: '3.8'

services:
  analyzer-web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - OMNIROUTE_GATEWAY_URL=${OMNIROUTE_GATEWAY_URL}
      - OMNIROUTE_API_KEY=${OMNIROUTE_API_KEY}
    restart: always
